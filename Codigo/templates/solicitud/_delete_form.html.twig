<form method="post" action="{{ path('app_solicitud_delete', {'idSolicitud': solicitud.idSolicitud}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitud.idSolicitud) }}">
    <button class="btn">Delete</button>
</form>
