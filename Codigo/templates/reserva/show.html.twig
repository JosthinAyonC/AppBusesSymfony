{% extends 'base.html.twig' %}

{% block title %}Reserva{% endblock %}

{% block body %}
    <h1>Reserva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReserva</th>
                <td>{{ reserva.idReserva }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ reserva.nombre }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ reserva.precio }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ reserva.fecha ? reserva.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reserva_index') }}">back to list</a>

    <a href="{{ path('app_reserva_edit', {'idReserva': reserva.idReserva}) }}">edit</a>

    {{ include('reserva/_delete_form.html.twig') }}
{% endblock %}
